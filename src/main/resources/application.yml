server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Geuniv/SpringConfigFile_v2
          default-label: main
          search-paths: poly